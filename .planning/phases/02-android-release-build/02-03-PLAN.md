---
phase: 02-android-release-build
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified: []
autonomous: false
requirements:
  - BUILD-05

must_haves:
  truths:
    - "The AAB is uploaded to the Play Console Internal Testing track"
    - "The release build installs on a physical Android device from the Internal Testing track"
    - "The installed app launches and runs correctly (basic smoke test)"
  artifacts:
    - path: "android/app/build/outputs/bundle/release/app-release.aab"
      provides: "The signed AAB to upload (produced by Plan 02)"
  key_links:
    - from: "app-release.aab"
      to: "Play Console Internal Testing"
      via: "Manual upload through Play Console web interface"
    - from: "Play Console Internal Testing"
      to: "Physical Android device"
      via: "Internal Testing opt-in link -> Play Store install"
---

<objective>
Upload the signed release AAB to the Google Play Console Internal Testing track and verify the app installs and runs on a physical Android device.

Purpose: BUILD-05 requires the release build to be uploaded to Internal Testing and confirmed installable on a real device. This is the final validation that the entire build pipeline produced a working release artifact before proceeding to Play Store assets and listing in later phases.

Output: Confirmed working release build accessible via Play Console Internal Testing.
</objective>

<execution_context>
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-release-build/02-CONTEXT.md
@.planning/phases/02-android-release-build/02-02-SUMMARY.md

<interfaces>
<!-- No code interfaces — this is a manual upload and device verification plan -->
<!-- The AAB artifact location (produced by Plan 02): -->
<!-- android/app/build/outputs/bundle/release/app-release.aab -->

App identity for Play Console:
- Package name: com.aifluent.app
- Version code: 1
- Version name: 1.0
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Upload AAB to Play Console Internal Testing (BUILD-05, D-06, D-07)</name>
  <files>None</files>
  <read_first>
    - android/app/build/outputs/bundle/release/app-release.aab (confirm the AAB artifact exists before asking user to upload)
    - capacitor.config.json (confirm appId is com.aifluent.app for Play Console reference)
  </read_first>
  <action>
    This is a blocking human action. The executor must confirm the AAB exists on disk, then pause and present the upload instructions to the user. Play Console has no public upload API for individual developers — this step cannot be automated.

    **Pre-check (automated):** Verify the AAB artifact exists:
    ```bash
    test -f android/app/build/outputs/bundle/release/app-release.aab && echo "AAB READY" || echo "AAB MISSING — run Plan 02 first"
    ```

    **Upload instructions for the user:**

    1. Open Google Play Console: https://play.google.com/console
    2. Select the "AI Fluent" app (package: `com.aifluent.app`)
       - If the app does not exist yet: "Create app" -> Title: "AI Fluent", Default language: English, App type: App, Free
    3. Navigate to: Testing -> Internal testing
    4. Click "Create new release"
    5. Upload the AAB file from: `C:\Users\hamou\ai-fluent\android\app\build\outputs\bundle\release\app-release.aab`
    6. Release name: `1.0 (1)` (or accept the default)
    7. Release notes: `Initial internal testing release`
    8. Click "Review release" then "Start rollout to Internal testing"
    9. Add yourself as a tester:
       - Go to Internal testing -> Testers tab
       - Create a testers list (e.g., "Internal") and add email: `hamoudi98@gmail.com`
       - Copy the opt-in URL that appears
  </action>
  <what-built>Plan 02 produced the signed AAB at `android/app/build/outputs/bundle/release/app-release.aab`. This task requires manual upload to Play Console (cannot be automated — Play Console has no public upload API for individual developers).</what-built>
  <how-to-verify>
    **Upload steps:**

    1. Open Google Play Console: https://play.google.com/console
    2. Select the "AI Fluent" app (package: `com.aifluent.app`)
       - If the app does not exist yet, create it: "Create app" -> Title: "AI Fluent", Default language: English, App type: App, Free
    3. Navigate to: **Testing** -> **Internal testing**
    4. Click **"Create new release"**
    5. Upload the AAB file from: `android/app/build/outputs/bundle/release/app-release.aab`
       - On Windows, the full path is: `C:\Users\hamou\ai-fluent\android\app\build\outputs\bundle\release\app-release.aab`
    6. Release name: `1.0 (1)` (or accept the default)
    7. Release notes: `Initial internal testing release`
    8. Click **"Review release"** then **"Start rollout to Internal testing"**
    9. Add yourself as a tester:
       - Go to **Internal testing** -> **Testers** tab
       - Create a testers list (e.g., "Internal") and add your email: `hamoudi98@gmail.com`
       - Copy the **opt-in URL** that appears

    **Verification:**
    - Play Console shows the release under Internal testing with status "Available" or "Rolling out"
    - Note the opt-in URL for the next task
  </how-to-verify>
  <verify>
    <automated>test -f android/app/build/outputs/bundle/release/app-release.aab && echo "AAB EXISTS" || echo "AAB MISSING"</automated>
    Expected: AAB EXISTS (confirms the artifact is available for upload)
  </verify>
  <acceptance_criteria>
    - User has confirmed the AAB is uploaded to Play Console Internal Testing
    - Play Console shows the release with status "Available" or "Rolling out"
    - User has provided the Internal Testing opt-in URL or confirmed "uploaded"
  </acceptance_criteria>
  <done>The signed AAB is uploaded to Play Console Internal Testing track and a tester list with the developer's email is configured.</done>
  <resume-signal>Paste the Internal Testing opt-in URL, or type "uploaded" if the release is visible in Play Console</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install and smoke test on physical device (BUILD-05, D-06)</name>
  <files>None</files>
  <read_first>
    - .planning/phases/02-android-release-build/02-CONTEXT.md (D-06 defines the smoke test scope)
  </read_first>
  <action>
    This is a blocking human verification. The executor must present the smoke test checklist to the user and wait for confirmation. The user must install the app from the Internal Testing track on their physical Android device and verify it works.

    **Installation steps to present:**
    1. Open the Internal Testing opt-in URL on the physical Android device
    2. Accept the tester invitation
    3. Install from Google Play Store link
    4. Run the 5-point smoke test checklist (see how-to-verify)
  </action>
  <what-built>The AAB was uploaded to Internal Testing in Task 1. Now verify it installs and runs on a real Android device.</what-built>
  <how-to-verify>
    **Installation steps:**

    1. On your physical Android device, open the Internal Testing opt-in URL in a browser
       - This is the URL from Task 1, or find it in Play Console: **Internal testing** -> **Testers** -> **Copy link**
    2. Accept the invitation to become a tester
    3. Click the Google Play Store link to install the app
    4. Wait for the app to download and install

    **Smoke test checklist** (basic functional verification, not full QA):

    1. **App launches:** Open "AI Fluent" from the app drawer. The app should show the dark-themed splash and then the login/signup screen (or World Map if already authenticated). No white flash on startup (Phase 1 fix LAYOUT-04).
    2. **Authentication works:** Sign in with your existing account. The app should load your profile and progress.
    3. **World Map renders:** The World Map screen shows all 7 location nodes. Summit node is visible at top (not overlapped by header -- Phase 1 fix LAYOUT-01). Base Camp is visible at bottom (not overlapped by nav bar -- Phase 1 fix LAYOUT-02).
    4. **Greeting correct:** The greeting says "Good afternoon, [your name]" (or appropriate time-of-day greeting), NOT "Good afternoon, AI" (Phase 1 fix LAYOUT-03).
    5. **Basic navigation:** Tap on a location node, confirm the lesson view opens. Press back, confirm you return to the World Map.

    **Expected result:** All 5 checks pass. The release build behaves identically to the debug build tested in Phase 1.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — user confirms 5-point smoke test on physical device"</automated>
    This is a human-only verification step. No automated command can test a physical device install from Play Console.
  </verify>
  <acceptance_criteria>
    - User has typed "verified" confirming all 5 smoke test checks pass
    - OR user has described specific issues that need to be addressed before proceeding
  </acceptance_criteria>
  <done>The release build installs from Internal Testing and runs correctly on a physical Android device (BUILD-05). All Phase 1 layout fixes confirmed intact in the release build.</done>
  <resume-signal>Type "verified" if all 5 smoke test checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local AAB -> Play Console | Upload transfers signed artifact to Google's infrastructure |
| Play Console -> device | Google re-signs with upload key and delivers via Play Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | AAB during upload | accept | Google Play Console verifies AAB signature on upload; rejects tampered bundles automatically |
| T-02-09 | Spoofing | Internal Testing access | mitigate | Only add known email (hamoudi98@gmail.com) to testers list; Internal Testing is not publicly discoverable |
</threat_model>

<verification>
- Play Console shows the Internal Testing release with status "Available" or "Rolling out"
- The app installs from the Internal Testing track on a physical device
- The app launches, authenticates, and displays the World Map correctly
- All Phase 1 layout fixes are intact in the release build
</verification>

<success_criteria>
BUILD-05 satisfied: Release build uploaded to Play Console Internal Testing track and confirmed installable and runnable on a physical Android device.
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-release-build/02-03-SUMMARY.md`
</output>
