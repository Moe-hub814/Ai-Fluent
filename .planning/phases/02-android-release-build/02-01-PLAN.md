---
phase: 02-android-release-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/.gitignore
autonomous: false
requirements:
  - BUILD-01
  - BUILD-02
user_setup:
  - service: password-manager
    why: "Keystore backup — losing this file means losing ability to update the app forever"
    env_vars: []
    dashboard_config:
      - task: "Store keystore file and credentials"
        location: "Bitwarden/1Password — create entry for AI Fluent Android Keystore"

must_haves:
  truths:
    - "keystore.properties is excluded from git tracking"
    - "*.keystore and *.jks files are excluded from git tracking"
    - "Keystore file and credentials are backed up in a password manager before git operations"
    - "Local keystore files still exist on disk after git rm --cached"
  artifacts:
    - path: "android/.gitignore"
      provides: "Keystore and credentials exclusion rules"
      contains: "keystore.properties"
  key_links:
    - from: "android/.gitignore"
      to: "android/ai-fluent-release.keystore"
      via: "*.keystore glob pattern"
      pattern: "^\\*\\.keystore$"
    - from: "android/.gitignore"
      to: "android/keystore.properties"
      via: "exact filename match"
      pattern: "^keystore\\.properties$"
---

<objective>
Secure the Android keystore and signing credentials by excluding them from git tracking and confirming the user has backed up the keystore to a password manager.

Purpose: BUILD-01 requires keystore files excluded from git. BUILD-02 requires keystore backed up to a secure offline location. The keystore is the single most critical file in the project — losing it means losing the ability to update the app on Google Play forever.

Output: Updated `android/.gitignore` with keystore exclusions, all three keystore-related files removed from git tracking (but preserved on disk), and confirmed user backup.
</objective>

<execution_context>
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-android-release-build/02-CONTEXT.md
@.planning/phases/02-android-release-build/02-PATTERNS.md

<interfaces>
<!-- No code interfaces — this is a git/config-only plan -->
<!-- Key file: android/.gitignore lines 55-58 contain the commented keystore block to uncomment -->

From android/.gitignore (lines 55-58, current state):
```
# Keystore files
# Uncomment the following lines if you do not want to check your keystore files in.
#*.jks
#*.keystore
```

Files currently tracked by git that must be untracked:
- android/ai-fluent-release.keystore
- android/app/ai-fluent-release.keystore
- android/keystore.properties
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Back up keystore to password manager (BUILD-02, D-03)</name>
  <files>None</files>
  <read_first>
    - android/ai-fluent-release.keystore (confirm file exists on disk before asking user to back it up)
    - android/keystore.properties (confirm credentials file exists)
  </read_first>
  <action>
    This is a blocking human action. The executor must pause and present the backup instructions to the user.
    No automated work is possible — the user must manually copy the keystore file and credentials to their password manager.

    **What the user must back up:**
    1. File: `android/ai-fluent-release.keystore` (2764 bytes) — attach to a password manager entry or copy to a secure offline location
    2. Credentials: Store Password = `Alsahlani1$`, Key Alias = `aifluent`, Key Password = `Alsahlani1$`

    **Why:** Google Play uses this keystore to verify app updates. If lost, the developer can never update the app on the same listing. There is no recovery.
  </action>
  <what-built>Nothing automated — this is a prerequisite human step before any git operations.</what-built>
  <how-to-verify>
    Back up the following to your password manager (Bitwarden, 1Password, etc.):

    1. **File to back up:** `android/ai-fluent-release.keystore` (2764 bytes)
       - Attach the file directly to a password manager entry, OR
       - Copy it to a known secure offline location (USB drive, encrypted folder)

    2. **Credentials to record in the same entry:**
       - Store Password: `Alsahlani1$`
       - Key Alias: `aifluent`
       - Key Password: `Alsahlani1$`

    3. **Why this matters:** Google Play uses this keystore to verify app updates come from the original developer. If this file is lost, you can never push an update to the same listing. There is no recovery mechanism.

    4. **Verify your backup:** Confirm you can access the stored entry and the file attachment is present.
  </how-to-verify>
  <verify>
    <automated>test -f android/ai-fluent-release.keystore && echo "KEYSTORE EXISTS" || echo "KEYSTORE MISSING"</automated>
    Expected: KEYSTORE EXISTS (file still on disk, user confirmed backup externally)
  </verify>
  <acceptance_criteria>
    - User has typed "backed up" confirming the keystore and credentials are stored in their password manager
    - `android/ai-fluent-release.keystore` still exists on disk (not accidentally deleted)
    - `android/keystore.properties` still exists on disk
  </acceptance_criteria>
  <done>Keystore file and credentials are backed up to a secure offline location (BUILD-02).</done>
  <resume-signal>Type "backed up" once the keystore file and credentials are saved in your password manager</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Exclude keystore files from git and remove from tracking (BUILD-01, D-01, D-02)</name>
  <read_first>
    - android/.gitignore (current state — lines 55-58 have the commented keystore block)
    - .planning/phases/02-android-release-build/02-PATTERNS.md (exact edit shape and command sequence)
  </read_first>
  <files>android/.gitignore</files>
  <action>
    **Step 1: Edit `android/.gitignore`** (per D-01)

    Modify lines 55-58 from:
    ```
    # Keystore files
    # Uncomment the following lines if you do not want to check your keystore files in.
    #*.jks
    #*.keystore
    ```

    To:
    ```
    # Keystore files
    # Uncomment the following lines if you do not want to check your keystore files in.
    *.jks
    *.keystore
    keystore.properties
    ```

    Specifically:
    - Line 57: Remove the leading `#` from `#*.jks` so it becomes `*.jks`
    - Line 58: Remove the leading `#` from `#*.keystore` so it becomes `*.keystore`
    - Add new line 59: `keystore.properties` (no leading comment, no blank line separator — stays within the same thematic block)

    **Step 2: Remove files from git tracking** (per D-02)

    Run these three commands in sequence:
    ```bash
    git rm --cached android/ai-fluent-release.keystore
    git rm --cached android/app/ai-fluent-release.keystore
    git rm --cached android/keystore.properties
    ```

    These commands remove the files from git's index (stop tracking) while keeping the physical files on disk intact. The local files must remain because the build pipeline needs them.

    **Step 3: Delete the redundant duplicate keystore**

    Per Claude's Discretion in CONTEXT.md: `android/app/ai-fluent-release.keystore` is byte-identical to `android/ai-fluent-release.keystore` (same MD5 `b031647219e094fb8f4cd9b76e2fb96e`, same size 2764 bytes). The `build.gradle` signing config resolves `storeFile` relative to the `android/` directory, so the `android/app/` copy is unused. Delete it from disk:
    ```bash
    rm android/app/ai-fluent-release.keystore
    ```

    **Step 4: Commit the security fix**

    Stage only the `.gitignore` change and the removals (the `git rm --cached` commands already stage the deletions):
    ```bash
    git add android/.gitignore
    git commit -m "fix(security): exclude keystore and credentials from git tracking"
    ```

    **Do NOT:** Rewrite git history, regenerate the keystore, or modify `build.gradle` or `keystore.properties`.
  </action>
  <verify>
    <automated>git ls-files | grep -E "keystore|\.jks" | wc -l</automated>
    Expected output: `0` (no keystore files tracked by git)

    Additional checks:
    - `test -f android/ai-fluent-release.keystore && echo "EXISTS" || echo "MISSING"` returns `EXISTS` (local file preserved)
    - `test -f android/keystore.properties && echo "EXISTS" || echo "MISSING"` returns `EXISTS` (local file preserved)
    - `test -f android/app/ai-fluent-release.keystore && echo "DUPLICATE EXISTS" || echo "DUPLICATE REMOVED"` returns `DUPLICATE REMOVED`
    - `grep "^keystore.properties" android/.gitignore` returns `keystore.properties` (line present, uncommented)
    - `grep "^\*\.keystore" android/.gitignore` returns `*.keystore` (line present, uncommented)
    - `grep "^\*\.jks" android/.gitignore` returns `*.jks` (line present, uncommented)
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep -E "keystore|\.jks"` returns zero lines
    - `android/.gitignore` contains the line `*.jks` (no leading `#`)
    - `android/.gitignore` contains the line `*.keystore` (no leading `#`)
    - `android/.gitignore` contains the line `keystore.properties`
    - `android/ai-fluent-release.keystore` exists on disk (file size > 0)
    - `android/keystore.properties` exists on disk (file size > 0)
    - `android/app/ai-fluent-release.keystore` does NOT exist on disk
    - `git log --oneline -1` contains "exclude keystore" or "keystore"
  </acceptance_criteria>
  <done>
    Keystore files and credentials are excluded from git tracking (BUILD-01). Local files remain on disk for the build pipeline. Redundant duplicate keystore deleted. Security commit recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo -> remote | Keystore and credentials must not be pushed to any remote |
| local disk -> build pipeline | Keystore must remain on disk for Gradle signing config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | android/keystore.properties | mitigate | D-01/D-02: Add to .gitignore and git rm --cached; credentials no longer in future commits |
| T-02-02 | Information Disclosure | android/ai-fluent-release.keystore | mitigate | D-01/D-02: Add *.keystore to .gitignore and git rm --cached; keystore no longer in future commits |
| T-02-03 | Denial of Service | keystore file loss | mitigate | D-03: Task 1 checkpoint requires user to back up keystore + credentials to password manager before any git operations |
| T-02-04 | Information Disclosure | git history contains keystore | accept | Repo is private; D-01 says do NOT rewrite git history — simplest approach is to stop tracking going forward. Risk accepted for private repo. |
</threat_model>

<verification>
- `git ls-files | grep -E "keystore|\.jks"` returns zero lines
- `android/.gitignore` contains uncommented `*.jks`, `*.keystore`, and `keystore.properties`
- Local keystore and properties files exist on disk
- Git log shows the security commit
</verification>

<success_criteria>
BUILD-01 satisfied: keystore.properties, *.keystore, and *.jks patterns are in android/.gitignore and all three files are untracked from git.
BUILD-02 satisfied: User confirmed keystore backup to password manager before git operations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-release-build/02-01-SUMMARY.md`
</output>
