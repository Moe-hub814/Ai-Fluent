---
phase: 03-play-store-assets
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/privacy-policy.html
autonomous: false
requirements: [ASSETS-05]

must_haves:
  truths:
    - "A privacy-policy.html file exists in the docs/ folder with all required disclosures"
    - "The policy names Supabase and Anthropic by name and discloses email collection, profile/progress storage, and AI-tool text processing"
    - "The policy is live and reachable at https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html"
  artifacts:
    - path: "docs/privacy-policy.html"
      provides: "Privacy policy static page with Google Play required disclosures"
      contains: "Anthropic"
  key_links:
    - from: "docs/privacy-policy.html"
      to: "GitHub Pages"
      via: "git push to main + Settings > Pages /docs config"
      pattern: "github.io/Ai-Fluent/privacy-policy.html"
---

<objective>
Write the privacy policy as a static HTML page, host it on GitHub Pages from the `docs/` folder, and verify it is publicly reachable at a stable URL.

Purpose: Google Play requires a privacy policy URL before a listing can be submitted (ASSETS-05, and Phase 4 STORE-08). For an AI app, omitting the Anthropic third-party processing disclosure is the most common rejection cause — this plan locks in every required disclosure.
Output: `docs/privacy-policy.html` committed and pushed, GitHub Pages enabled, live URL `https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html` verified loading.
</objective>

<execution_context>
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hamou/ai-fluent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-play-store-assets/03-CONTEXT.md
@.planning/phases/03-play-store-assets/03-RESEARCH.md
@.planning/phases/03-play-store-assets/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/privacy-policy.html with all required disclosures</name>
  <files>docs/privacy-policy.html</files>
  <read_first>
    - .planning/phases/03-play-store-assets/03-RESEARCH.md (Privacy Policy Requirements; Code Examples — Privacy Policy HTML Template; Pitfall 6: Missing Anthropic API Disclosure)
    - .planning/phases/03-play-store-assets/03-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/03-play-store-assets/03-PATTERNS.md (static-page analog — public/index.html structure; Branding Colors; Link Color Convention)
    - public/index.html (head/meta and footer structure analog)
  </read_first>
  <action>
    Create the `docs/` directory (it does not yet exist — GitHub Pages needs the folder to exist before it can be enabled), then create `docs/privacy-policy.html`.

    Use the verified HTML template in 03-RESEARCH.md "Code Examples — Privacy Policy HTML Template" as the base. It is a self-contained static HTML file: `<!DOCTYPE html>`, `<html lang="en">`, charset + viewport meta, an inline `<style>` block (light background, system fonts, max-width 720px centered, line-height 1.6). Do NOT add `maximum-scale`/`user-scalable=no` — the policy page must allow zoom for accessibility.

    Title: `AI Fluent — Privacy Policy`. Set the effective date and "Last updated" date to `2026-05-19`.

    The policy MUST include these sections with these exact disclosures (D-12, D-13):
    1. **What We Collect** — email address (account creation); display name; learning data (lessons completed, streak, achievements); AI tool inputs (text typed in AI Tools, sent to Anthropic's Claude API, not stored by AI Fluent).
    2. **How We Use It** — account authentication/management; saving/displaying learning progress; powering AI-generated responses in AI Tools.
    3. **Third-Party Services** — name **Supabase** (link https://supabase.com/privacy) for auth + database hosting; name **Anthropic** (link https://www.anthropic.com/legal/privacy) for the Claude AI API. Include verbatim: "Anthropic does not use API data for model training. Per Anthropic's standard policy, API inputs and outputs may be retained for up to 7 days for security purposes, then deleted."
    4. **What We Do Not Do** — no selling data; no advertising networks or advertising identifiers; no analytics or tracking SDKs.
    5. **Data Retention** — account data retained until account deletion; Anthropic API processing subject to Anthropic's 7-day retention, not stored by AI Fluent.
    6. **Security** — all data transmitted over HTTPS; Supabase enforces encryption at rest.
    7. **Contact** — developer email `hamoudi98@gmail.com` as a `mailto:` link.

    Styling: light background (`background:#fff; color:#222`), `h1` in `#060D1A` (optionally a thin `#D4A55A` gold accent on the page header), inline links in `#0066cc` (NOT gold — gold fails contrast on white). Minimal footer with a copyright line.

    SECURITY: this file is a public static page. It MUST NOT contain any secrets — no API keys, no Supabase service keys, no keystore passwords. Only the publishable contact email and public service names belong here.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content docs/privacy-policy.html -Raw; if ($c -match 'Anthropic' -and $c -match 'Supabase' -and $c -match '7 days' -and $c -match 'hamoudi98@gmail.com' -and $c -match '<!DOCTYPE html>') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/privacy-policy.html` exists and starts with `<!DOCTYPE html>`
    - File contains the strings "Anthropic", "Supabase", "7 days", and "hamoudi98@gmail.com"
    - File contains all 7 required section headings (What We Collect, How We Use It, Third-Party Services, What We Do Not Do / No Advertising, Data Retention, Security, Contact)
    - File contains an effective date of `2026-05-19`
    - File contains NO secrets — grep for `service_role`, `SUPABASE_SERVICE`, `keystore`, `password`, `apikey` returns no matches
  </acceptance_criteria>
  <done>A self-contained, secret-free privacy policy HTML page with every Google Play required disclosure exists at `docs/privacy-policy.html`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Commit, push, enable GitHub Pages, and verify the live URL</name>
  <files>docs/privacy-policy.html</files>
  <read_first>
    - .planning/phases/03-play-store-assets/03-RESEARCH.md (Pattern 3: GitHub Pages Hosting Setup; Pitfall 2: GitHub Pages Not Live When Play Console Verifies)
    - .planning/phases/03-play-store-assets/03-CONTEXT.md (D-11)
  </read_first>
  <action>
    What this checkpoint produces:
    `docs/privacy-policy.html` was created in Task 1. The commit/push can be automated, but enabling GitHub Pages requires a click in the GitHub web UI Settings — that part is the human action.
  
  

    Steps to perform:
    AUTOMATED (executor runs these):
    1. Stage and commit only the policy file:
       `git add docs/privacy-policy.html`
       `git commit -m "docs(03): add privacy policy page for GitHub Pages hosting"`
    2. Push to main: `git push origin main`

    HUMAN ACTION (user does these in the browser):
    3. Open the GitHub repo `Moe-hub814/Ai-Fluent` → Settings → Pages.
    4. Under "Build and deployment": Source = "Deploy from a branch".
    5. Branch = `main`, Folder = `/docs`. Click Save.
    6. Wait 2-10 minutes for the first Pages build. In the repo "Actions" tab, wait for the "pages build and deployment" run to show a green checkmark.
    7. In a browser, open `https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html` and confirm the page loads (not a 404).

    VERIFY (executor confirms reachability once the user signals the build is green):
    `powershell -Command "(Invoke-WebRequest -Uri 'https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html' -UseBasicParsing).StatusCode"`
    Must return `200`. The response body should contain "Privacy Policy" and "Anthropic".

    Do NOT proceed to Phase 4 until the URL returns 200 — Play Console's privacy policy verifier will fail against a 404.
  
  </action>
  <acceptance_criteria>
    - `docs/privacy-policy.html` is committed and `git log -1 --name-only` shows it pushed to main
    - GitHub Pages is configured to deploy from `main` branch `/docs` folder
    - `Invoke-WebRequest https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html` returns HTTP `200`
    - The fetched page body contains "Anthropic" and "Privacy Policy"
  </acceptance_criteria>
  <verify>
    <automated>see acceptance_criteria — verified manually by checkpoint reviewer</automated>
  </verify>
  <done>All acceptance_criteria above are satisfied; user confirms via resume-signal.</done>
  <resume-signal>Type "live" once the URL returns 200 in a browser, or describe the GitHub Pages build status.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public internet | The `docs/` folder is served publicly by GitHub Pages — even from a private repo, Pages content is publicly accessible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | docs/privacy-policy.html on public GitHub Pages | mitigate | The file is reviewed to contain only public information — contact email and named third-party services. Task 1 acceptance criteria explicitly grep for and reject secrets (`service_role`, `SUPABASE_SERVICE`, `keystore`, `password`, `apikey`) before commit. |
| T-03-08 | Information Disclosure | Inaccurate or incomplete data disclosure | mitigate | The policy must disclose Anthropic API processing and 7-day retention verbatim from official Anthropic docs (Pitfall 6). An incomplete policy risks post-launch Play Store rejection — Task 1 acceptance criteria enforce presence of all required disclosures. |
| T-03-09 | Repudiation | Stale GitHub Pages URL at Play Console verification time | mitigate | Task 2 requires confirming HTTP 200 from the live URL before Phase 4 proceeds, preventing the "URL 404 at review" failure mode. |
</threat_model>

<verification>
- `docs/privacy-policy.html` exists, is secret-free, and contains all required disclosures (Anthropic, Supabase, 7-day retention, email, no-tracking)
- GitHub Pages enabled on `main` `/docs`
- `https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html` returns HTTP 200
</verification>

<success_criteria>
A privacy policy disclosing email collection, Supabase storage, and Anthropic API processing is live at a stable public URL (`https://Moe-hub814.github.io/Ai-Fluent/privacy-policy.html`) — meeting ASSETS-05 and ready for the Play Console "App content" entry in Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/03-play-store-assets/03-04-SUMMARY.md`
</output>
